server:
  host: localhost
  port: 50051

jwks:
  host: localhost
  port: 8081

database:
  host: localhost
  port: 5451
  user: banking_auth
  password: 123
  dbname: banking_auth
  ssl_mode: disable

admin:
  email: admin@banking.local
  password: "admin123"

upstreams:
  customer: localhost:50052

jwt:
  # Каталог ключей подписи. Активный - последний по имени файла, остальные
  # остаются в JWKS и продолжают проверять уже выданные токены.
  keys_dir: ./configs/jwt_keys
  access_ttl: 15m
  refresh_ttl: 720h

request_timeout: 30s